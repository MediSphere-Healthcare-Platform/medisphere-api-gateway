server:
  port: 8080

spring:
  application:
    name: medisphere-api-gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/

logging:
  level:
    com.medisphere.apigateway: DEBUG
    org.springframework.web: DEBUG
